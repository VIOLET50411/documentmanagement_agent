"""Working Memory — Per-request state managed by LangGraph AgentState."""
# This is implemented directly in the AgentState TypedDict in supervisor.py
# No separate class needed — LangGraph manages this natively.
